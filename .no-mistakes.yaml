# CI is live: .github/workflows/ci.yml (test + build on four platforms, runs on
# PRs and pushes to main) and release.yml (tag-triggered). The no_ci declaration
# that previously told the no-mistakes gate to treat a zero-check PR head as
# all-checks-passed is removed: real checks now register on PRs.
